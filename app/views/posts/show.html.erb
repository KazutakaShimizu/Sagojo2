<p>
	<strong>Title:</strong>
	<%= @post.title %>
</p>


	<strong>Content:</strong>
	<%== @post.content %>

<p>
	<strong>この記事のライター:</strong>
	<%= @post.name %>
</p>

<%= link_to '問い合わせ', inquiry_path %>
<% if user_signed_in? %>
	<br>
	<%= link_to 'Edit', edit_post_path(@post) %> |
	<%= link_to 'Back', posts_path %>
	</br>
<% end %>